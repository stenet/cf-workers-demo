{
  "name": "workers-test",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230404.0",
    "typescript": "^5.0.4",
    "wrangler": "2.15.1"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev --local --persist",
    "start:remote": "wrangler dev",
    "deploy": "wrangler publish"
  }
}
